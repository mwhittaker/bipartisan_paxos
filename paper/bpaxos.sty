
% The set of commands. x, y, z \in \Cmd.
\newcommand{\Cmd}{Cmd}

% The conflict relation.
\newcommand{\conflict}{D}

% Dependency sets.
\newcommand{\deps}[1]{\text{deps}(#1)}

% Noop.
\newcommand{\noop}{\text{noop}}

% Quorum.
\newcommand{\Quorum}{\mathcal{Q}}
